
  /**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    /* config options here */
    images: {
        loader: "imgix",
        path: "https://noop/",
    },

  }
  
  module.exports = nextConfig